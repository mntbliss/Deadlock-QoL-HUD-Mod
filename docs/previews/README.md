# Preview clips

Drop short loops here. The README embeds **GIFs** (GitHub and most markdown previews ignore relative `<video>` tags).

| File | Feature |
| --- | --- |
| `hp-bar.gif` | Bottom-center HP, shields, souls, level jar |
| `minimap.gif` | Rounded minimap |
| `heart-crosshair.gif` | Heart reticle |
| `minion-bars.gif` | Trooper / minion HP bars |
| `inventory.gif` | Idle dotted inventory + shop restore |

Keep clips small (a few seconds, muted, cropped to the HUD).
