# Preview clips

Drop short loops here. GitHub and the README pick them up by filename.

| File | Feature |
| --- | --- |
| `hp-bar.webm` or `hp-bar.gif` | Bottom-center HP, shields, souls, level jar |
| `minimap.webm` or `minimap.gif` | Rounded minimap |
| `heart-crosshair.webm` or `heart-crosshair.gif` | Heart reticle |
| `minion-bars.webm` or `minion-bars.gif` | Trooper / minion HP bars |
| `inventory.webm` or `inventory.gif` | Idle dotted inventory + shop restore |

Keep clips small (a few seconds, muted, cropped to the HUD).
